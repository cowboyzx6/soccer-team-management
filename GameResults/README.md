# Game Results

One folder per season. Spring = January–June, Fall = July–December.

Each file is **cumulative for its season**. The highest game number in a folder has every game of that season.

**Restoring on a new device:** open the current season's folder, then use **Restore Backup** in the app with the file that has the highest `Game_N`.
